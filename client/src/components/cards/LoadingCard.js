import { Skeleton } from 'antd';
import React from 'react'
  

const LoadingCard = () => {
    return (
      <div>
        <Skeleton active></Skeleton>;
      </div>
    );
}

export default LoadingCard
